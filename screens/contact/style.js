import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
      flex: 1,
      backgroundColor: '#fff',
    },
    container2: {
      flex: 1,
      backgroundColor: '#fff',
      alignItems: 'center',
    },
    image: {
      width: 300,
      height: 300,
      justifyContent: 'center',
      alignItems: 'center',
    },
  });
  